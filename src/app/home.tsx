'use client'
// Placeholder — replaced with the full onboarding/shelf/session UI in task 7.
export default function Home() {
  return (
    <main style={{ display: 'grid', placeItems: 'center', height: '100vh' }}>
      <p style={{ color: 'var(--color-text-secondary)', fontFamily: 'var(--font-serif)', fontStyle: 'italic' }}>
        Worldview
      </p>
    </main>
  )
}
